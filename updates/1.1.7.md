# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added on image post section a button to close the opened dialog for uploading an image on the pinboard - it used to close only through `ESC` key.
2. Added a new feature for creating your own notes through a text editor.


## Modified Files:
- client/tp_client_nui.lua
- server/tp-server_main.lua
- html **replace the whole folder**
- fxmanifest.lua

## Notes

- Add the following code on mysql / heidi query for the new database columns

```sql
ALTER TABLE `tp_pinboard`
ADD COLUMN IF NOT EXISTS `textarea` longtext DEFAULT NULL,
ADD COLUMN IF NOT EXISTS `background_color` longtext DEFAULT NULL;
```

## Screenshots
<img width="1015" height="887" alt="εικόνα" src="https://github.com/user-attachments/assets/ad759360-9b72-454f-bdb6-f7c9f8acc1e7" />
<img width="1657" height="1079" alt="εικόνα" src="https://github.com/user-attachments/assets/e6e9bbfb-099f-411a-8709-81d83a2e3d6c" />
